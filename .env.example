# Copy to .env and fill locally. Never commit .env.
# OpenAI-compatible strong-model API used by the generator and judge.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
GENERATOR_MODEL=
JUDGE_MODEL=

# Optional: only needed when publishing the adapter to Hugging Face.
HF_TOKEN=
